import {$,formatDate,today} from '../core/utils.js';import {state} from '../core/state.js';
export function renderDaily(){const d=new Date();const dates=[];for(let i=-2;i<=4;i++){const x=new Date(d);x.setDate(d.getDate()+i);const iso=`${x.getFullYear()}-${String(x.getMonth()+1).padStart(2,'0')}-${String(x.getDate()).padStart(2,'0')}`;dates.push(`<div class="date ${iso===today()?'active':''} ${state.history.some(h=>h.date===iso)?'done':''}"><small>${x.toLocaleDateString('pt-BR',{weekday:'short'}).replace('.','')}</small><b>${x.getDate()}</b></div>`)}$('#dateStrip').innerHTML=dates.join('');$('#dailyStreak').textContent=state.streak;const todayHist=state.history.filter(h=>h.date===today());const mins=todayHist.reduce((a,h)=>a+h.minutes,0);const sets=todayHist.reduce((a,h)=>a+h.sets,0);const xp=todayHist.reduce((a,h)=>a+h.xp,0);$('#todayStatus').textContent=`${mins} min`;$('#dailyMinutes').textContent=mins;$('#dailyMetrics').innerHTML=`<div class="mini-metric">⚡ <b>${xp}</b> XP hoje</div><div class="mini-metric">✓ <b>${sets}</b> séries</div>`;$('#dailyTitle').textContent=state.lastWorkout===today()?'Você treinou hoje.':'Continue a sequência.';$('#dailyMessage').textContent=state.lastWorkout===today()?'Ótimo. Se quiser, você pode fazer um treino leve ou descansar.':'Cada treino conta. O próximo começa com um toque.';$('#history').innerHTML=state.history.length?state.history.slice(0,12).map(h=>`<div class="history-item"><span class="history-icon">🔥</span><div><b>${h.name}</b><small>${formatDate(h.date)} • ${h.sets} séries • ${h.minutes} min</small></div><strong>+${h.xp}</strong></div>`).join(''):'<div class="empty">Seu histórico aparecerá aqui após o primeiro treino.</div>'}
